
h3 1 - Open console in your browser
p This exercise shows the results in browser console.

ul
  li open the exercise by clicking #[strong Open Exercise]
  li open browser console.
  li you will see there an error

snippet.
  Uncaught TypeError: gen1 is not a function

h3 2 - Implement the simple generator

p.
  You will try to utilize closure when implementing simple generator.

ul
  li Check its usage which is already prepared in #[strong app.js]

snippet.
  var gen1 = generator(0);

  console.log(gen1());
  console.log(gen1());
  console.log(gen1());

h4 2.1 - Make it return a function
ul
  li if you look on the function named #[strong generator], it accepts single parameter (initialValue)
  li according to the usage, it should return another function
  li but currently its not - this is the reason of the error in console

ul
  li let #[strong generator] function return another function

solution.
  function generator(initialValue) {

    return function() {

    };
  }

h4 2.2 - Return and increase the value
p It doesn't fail anymore, but there is no returned value

ul
  li return the increased value of #[strong initialValue]

hint Use the postincrement - #[strong value]++

solution.
  function generator(initialValue) {

    return function() {
      return initialValue++;
    };
  }

h3 3 - Check the results

p Currently browser console shows the final results

snippet.
  0
  1
  2

p.success Congratulations you have finished first exercise
